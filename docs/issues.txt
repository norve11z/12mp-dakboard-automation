- the overall theme of admin page need to look less slop
- still need a non gameday page


Correctness / stability

    Timezone: add-game fixed, but display-state.ts and assign.ts compute localDate() inconsistently vs. Vercel UTC — audit all date math to use one Chicago-aware helper
    import_logs.ran_at displayed as UTC in dashboard; format to Central
    Duplicate first names collide (e.g., two "Sam"): first-name-last-initial should disambiguate but doesn't when both first names match

UX polish

    Panel preview cards don't show crew count / crew-call time — add
    No confirmation toast for successful actions (only shows in gray text)
    Add-Game modal: crew textarea is fragile; a table with add-row is more forgiving
    No indicator on /admin/positions that positions used in shifts have no mapping (missing labels)

Data hygiene

   *** No way to see all imported shifts / raw shift table for debugging

Feature gaps

    Vercel cron not set up yet (vercel.json) — no auto import in prod
    No display auto-refresh (DAKboard iframe caches; needs meta refresh or interval)
    Schedule times editor doesn't show computed preview times (hard to verify)
    Position map editor is disconnected from what positions actually exist in shifts
